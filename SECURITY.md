# الأمان والخصوصية — أدوات المعلم

هذا المستند يشرح أين تُحفظ الأسرار، وما الذي يجب ألا يُرفع إطلاقاً،
وسياسة بيانات الطلاب، وكيف تُمنح صلاحية الإدارة.

---

## 1) أين توجد الأسرار

| البيئة | المكان | مرفوع إلى Git؟ |
|---|---|---|
| التطوير المحلي | ملف `.dev.vars` في جذر المشروع | ❌ مُستثنى في `.gitignore` |
| الإنتاج | Cloudflare Secrets (`wrangler secret put`) | ❌ لا تُخزَّن في المستودع |
| قالب مرجعي | `.dev.vars.example` و `.env.example` | ✅ لكن بقيم Placeholder فقط |

**لا يوجد أي سرّ داخل:**

- كود الواجهة (`src/`) — الواجهة لا تعرف التوكنات ولا أسرار OAuth.
- `wrangler.jsonc` — يحتوي فقط `E2E_TEST_MODE: "false"` وهو ليس سرّاً.
- أي ملف داخل حزمة النشر النهائية.

الأسرار الحسّاسة: `GOOGLE_CLIENT_SECRET`, `BETTER_AUTH_SECRET`,
`TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`.

---

## 2) ما الذي يجب ألا يُرفع أبداً

```
.dev.vars
.env
.env.local
.wrangler/
node_modules/
dist/
```

كلها مُدرجة في `.gitignore`.

> **ملاحظة على مجلد البناء:** إضافة Cloudflare لـ Vite تنسخ ملف `.dev.vars`
> إلى `dist/<worker-name>/.dev.vars` لاستخدامه في `vite preview` المحلي فقط.
> هذا الملف **لا يُرفع** ضمن الأصول العامة (`dist/client`) ولا يدخل في حزمة الـ Worker،
> لكن لا تُشارك مجلد `dist/` مع أحد ولا تضعه في أرشيف تُسلّمه لغيرك.
> (تم التحقق: لا يحتوي `dist/client` ولا `dist/<worker-name>/index.js` أي سرّ.)

### ⚠️ تنبيه يخصّ هذا المستودع

الكود السابق لهذا المستودع (المؤرشف في `legacy/main.py`) كان يحتوي
**Telegram Bot Token مكتوباً داخل الكود**، وهو موجود أيضاً في **تاريخ Git**.

حذف الملف الآن **لا يمسح** التوكن من التاريخ. الإجراء الصحيح:

1. افتح BotFather ونفّذ `/revoke` للبوت القديم فوراً.
2. أنشئ توكناً جديداً واستخدمه عبر `wrangler secret put TELEGRAM_BOT_TOKEN` فقط.
3. لا تضع التوكن الجديد في أي ملف كود.

---

## 3) سياسة بيانات الطلاب

> **القاعدة: بيانات الطلاب لا تغادر جهاز المعلم.**

| البيانات | أين تُحفظ | تصل للخادم؟ |
|---|---|---|
| أسماء الطلاب | `localStorage` في متصفّح المعلم | ❌ |
| الدرجات | `localStorage` | ❌ |
| المهارات والملاحظات | `localStorage` | ❌ |
| محتوى المستند كاملاً | ذاكرة الصفحة + `localStorage` | ❌ |
| تفضيلات القالب والألوان | D1 (جدول `user_preferences`) | ✅ |
| أحداث الاستخدام (Metadata) | D1 (جدول `usage_events`) | ✅ |

### ما الذي يُسجَّل فعلياً في `usage_events`

فقط هذه الأعمدة، ولا شيء غيرها:

```
id, user_id, tool_id, event_type, template_id, primary_color, created_at
```

أنواع الأحداث المقبولة محصورة في قائمة ثابتة يتحقّق منها Zod على الخادم:
`login`, `telegram_linked`, `subscription_verified`, `subscription_failed`,
`tool_opened`, `export_pdf`, `export_png`, `print`, `template_selected`.

أي حقل إضافي يرسله العميل يُتجاهَل ولا يُخزَّن — وهذا مُغطّى باختبار تكامل
(`tests/api.integration.test.ts`) يتحقّق من أسماء أعمدة الصف المخزَّن فعلياً.

**لا يوجد حدث عند كل ضغطة مفتاح.** الأحداث المهمة فقط، مع منع تكرار
لمدة 30 ثانية في الواجهة، وحدّ 60 حدثاً في الدقيقة لكل مستخدم على الخادم.

### حذف البيانات

- زر **«مسح بيانات الأداة»** داخل كل أداة يمسح بياناتها من الجهاز.
- زر **«مسح بيانات جميع الأدوات»** في صفحة الحساب يمسح كل شيء من الجهاز.

### لا توجد معالجة بالذكاء الاصطناعي

النسخة الأولى لا ترسل أي بيانات إلى أي واجهة ذكاء اصطناعي.

---

## 4) المصادقة والجلسات

- **Google OAuth عبر Better Auth** — تبادل الرمز وقيمة `state` يتمّان في الخادم بالكامل.
- `GOOGLE_CLIENT_SECRET` لا يصل إلى المتصفّح إطلاقاً.
- كوكي الجلسة:
  - `HttpOnly` — غير قابل للقراءة من JavaScript.
  - `SameSite=Lax` — حماية من CSRF عبر التنقّل.
  - `Secure` — مفعّل تلقائياً عندما يكون `BETTER_AUTH_URL` على HTTPS.
  - `Path=/`
- مدة الجلسة 30 يوماً مع تجديد كل 24 ساعة، وكاش قصير (5 دقائق) لتقليل قراءات D1.

---

## 5) ربط تيليجرام — نموذج التهديد والحماية

التهديد الأساسي: أن يدّعي مستخدم أن معرّف تيليجرام معيّن يخصّه.

**الحل: الربط لا يحدث إلا من داخل الـ Webhook القادم من تيليجرام.**

| الحماية | التنفيذ |
|---|---|
| التحقق من مصدر الـ Webhook | ترويسة `X-Telegram-Bot-Api-Secret-Token` تُقارَن بـ `TELEGRAM_WEBHOOK_SECRET` بمقارنة **ثابتة الزمن** (`timingSafeEqual`) |
| توكن عشوائي قوي | 32 بايت من `crypto.getRandomValues` بصيغة base64url |
| لا نخزّن التوكن | نخزّن بصمة **SHA-256** فقط في `telegram_link_tokens.token_hash` |
| صلاحية قصيرة | 10 دقائق (`expires_at`) |
| استخدام واحد | `UPDATE ... WHERE id = ? AND used_at IS NULL` — عملية ذرّية تمنع إعادة الاستخدام حتى مع طلبين متزامنين (Replay/Race) |
| توكن واحد فعّال | إنشاء توكن جديد يحذف التوكنات السابقة غير المستخدمة لنفس المستخدم |
| المعرّف الرقمي هو المفتاح | لا نعتمد `username` أبداً لأنه قابل للتغيير |
| حساب واحد لحساب واحد | فهرس `UNIQUE` على `telegram_user_id` و `user_id` |
| لا ثقة في العميل | لا يوجد أي مسار عام يقبل `telegram_user_id` من المتصفّح |

---

## 6) صلاحيات الإدارة

- الأدوار: `user` و `admin` في عمود `user.role`.
- الترقية إلى `admin` تحدث **فقط** داخل معالج الـ Webhook، وفقط عندما يطابق
  `telegram_user_id` القادم من تيليجرام قيمة `ADMIN_TELEGRAM_ID`.
- حقل `role` مُعرَّف في Better Auth بـ `input: false`، أي أن العميل **لا يستطيع**
  إرساله عند التسجيل أو التحديث (مُغطّى باختبار تكامل).
- كل نقطة API إدارية تفحص الدور من **قاعدة البيانات** في كل طلب
  (`requireAdmin` في `worker/lib/gate.ts`) — إخفاء الزر في الواجهة ليس حماية.
- `/admin` لمستخدم عادي: صفحة 403 في الواجهة، و`403` من الـ API.
- **الإدمن لا يتجاوز بوابة الاشتراك**: يخضع لنفس شروط `canUseTools` تماماً.

---

## 7) التحقق من المدخلات ومنع الحقن

- **SQL Injection:** كل الاستعلامات Parameterized (`?1, ?2 …`) عبر
  `db.prepare().bind()`. لا يوجد أي بناء SQL بدمج نصوص في المشروع.
- **التحقق من الشكل:** Zod على كل جسم طلب (`/api/events`, `/api/me/preferences`)،
  مع قوائم قيم مغلقة (enum) وأنماط صارمة للألوان (`^#[0-9a-fA-F]{6}$`).
- **XSS:** لا يوجد أي استخدام لـ `dangerouslySetInnerHTML` في المشروع.
  كل النصوص تمرّ عبر هروب React التلقائي.
- **تجاوز الحصّة:** حدّ 60 حدثاً/دقيقة لكل مستخدم، وفاصل 8 ثوانٍ بين طلبات
  التحقق اليدوي من الاشتراك.
- **رسائل الأخطاء:** رسائل عربية عامة فقط. لا يُعرض أي Stack Trace أو تفصيل تقني
  للمستخدم؛ التفاصيل تُسجَّل في سجلات الخادم فقط.

للتحقق من عدم تسريب الأسرار في الردود يوجد اختبار تكامل يفحص ذلك صراحةً.

---

## 8) وضع الاختبار (E2E)

لتشغيل اختبارات E2E دون Google وتيليجرام حقيقيين، يوجد وضع اختبار يفعّل
تسجيل الدخول بالبريد **فقط**.

شروط تفعيله (يجب توفّرهما معاً):

```
E2E_TEST_MODE=true
E2E_TEST_SECRET=<قيمة غير فارغة>
```

في الإنتاج:

- `wrangler.jsonc` يضبط `E2E_TEST_MODE: "false"`.
- لا تضف سرّاً باسم `E2E_TEST_SECRET` إطلاقاً.
- `/api/health` يُظهر `"testMode": false` — استخدمه للتأكد بعد كل نشر.

---

## 9) قائمة فحص أمني قبل كل نشر

- [ ] `git status` نظيف ولا يحتوي `.dev.vars`.
- [ ] `grep -rn "AAH\|AAE\|bot[0-9]\{8,\}:" src worker shared` لا يُرجع شيئاً.
- [ ] `npx wrangler secret list` يعرض كل الأسرار المطلوبة.
- [ ] `curl <URL>/api/health` يُرجع `ok:true` و `testMode:false`.
- [ ] `/admin` بحساب عادي يعطي 403.
- [ ] `getWebhookInfo` يعرض الرابط الصحيح بلا أخطاء.
- [ ] لا يوجد `console.log` يطبع توكناً أو سرّاً.

---

## 10) الإبلاغ عن ثغرة

إن اكتشفت ثغرة، لا تفتح Issue عامة. راسل مالك المشروع مباشرة عبر قناة المنصة
على تيليجرام مع وصف الخطوات، وأمهِله وقتاً معقولاً للإصلاح قبل أي إفصاح.
